extends layout

block content
    p Orchestrator allows you to synchronize your browser. Scrolling, navgating and mouse events are propagated to all connected browsers. 
    section
        h1 Start a new session
        form(action="/", method="POST")
            p
                label Username
                    input(name="username")
            p
                label Password
                    input(name="password", type="password")
            p
                button(type=submit) Start
        if currentUrl
            p
                a(href="#{currentUrl}") Join current session
